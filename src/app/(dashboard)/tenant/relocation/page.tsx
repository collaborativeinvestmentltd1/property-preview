import PlaceholderPage from "@/components/dashboard/PlaceholderPage";

export default function TenantRelocationPage() {
  return (
    <PlaceholderPage
      eyebrow="Relocation"
      title="Relocation planning"
      description="Manage your moving schedule, payment transfers, and lease transitions with a single toolset."
      backHref="/tenant"
    />
  );
}
